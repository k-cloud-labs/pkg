---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: clustervalidatepolicies.policy.kcloudlabs.io
spec:
  group: policy.kcloudlabs.io
  names:
    kind: ClusterValidatePolicy
    listKind: ClusterValidatePolicyList
    plural: clustervalidatepolicies
    shortNames:
    - cvp
    singular: clustervalidatepolicy
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterValidatePolicy represents the cluster-wide policy that
          validate a group of resources.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ClusterValidatePolicySpec defines the desired behavior of
              ClusterValidatePolicy.
            properties:
              resourceSelectors:
                description: ResourceSelectors restricts resource types that this
                  validate policy applies to. nil means matching all resources.
                items:
                  description: ResourceSelector the resources will be selected.
                  properties:
                    apiVersion:
                      description: APIVersion represents the API version of the target
                        resources.
                      type: string
                    kind:
                      description: Kind represents the Kind of the target resources.
                      type: string
                    labelSelector:
                      description: A label query over a set of resources. If name
                        is not empty, labelSelector will be ignored.
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: A label selector requirement is a selector
                              that contains values, a key, and an operator that relates
                              the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: operator represents a key's relationship
                                  to a set of values. Valid operators are In, NotIn,
                                  Exists and DoesNotExist.
                                type: string
                              values:
                                description: values is an array of string values.
                                  If the operator is In or NotIn, the values array
                                  must be non-empty. If the operator is Exists or
                                  DoesNotExist, the values array must be empty. This
                                  array is replaced during a strategic merge patch.
                                items:
                                  type: string
                                type: array
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: matchLabels is a map of {key,value} pairs.
                            A single {key,value} in the matchLabels map is equivalent
                            to an element of matchExpressions, whose key field is
                            "key", the operator is "In", and the values array contains
                            only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                    name:
                      description: Name of the target resource. Default is empty,
                        which means selecting all resources.
                      type: string
                    namespace:
                      description: Namespace of the target resource. Default is empty,
                        which means inherit from the parent object scope.
                      type: string
                  required:
                  - apiVersion
                  - kind
                  type: object
                type: array
              validateRules:
                description: ValidateRules defines a collection of validate rules
                  on target operations.
                items:
                  description: ValidateRuleWithOperation defines the validate rules
                    on operations.
                  properties:
                    cue:
                      description: Cue represents validate rules defined with cue
                        code.
                      type: string
                    renderedCue:
                      description: RenderedCue represents validate rule defined by
                        Template. Don't modify the value of this field, modify Rules
                        instead of.
                      type: string
                    targetOperations:
                      description: Operations is the operations the admission hook
                        cares about - CREATE, UPDATE, DELETE, CONNECT or * for all
                        of those operations and any future admission operations that
                        are added. If '*' is present, the length of the slice must
                        be one. Required.
                      items:
                        description: Operation is the type of resource operation being
                          checked for admission control
                        type: string
                      type: array
                    template:
                      description: Template of condition which defines validate cond,
                        and it will be rendered to CUE and store in RenderedCue field,
                        so if there are any data added manually will be erased.
                      properties:
                        cond:
                          allOf:
                          - enum:
                            - Equal
                            - NotEqual
                            - Exist
                            - NotExist
                            - In
                            - NotIn
                            - Gt
                            - Gte
                            - Lt
                            - Lte
                          - enum:
                            - Equal
                            - NotEqual
                            - Exist
                            - NotExist
                            - In
                            - NotIn
                            - Gt
                            - Gte
                            - Lt
                            - Lte
                          description: Cond represents type of condition (e.g. Equal,
                            Exist)
                          type: string
                        dataRef:
                          description: DataRef represents for data reference from
                            current or remote object. Need specify the type of object
                            and how to get it.
                          properties:
                            from:
                              allOf:
                              - enum:
                                - current
                                - old
                                - k8s
                                - owner
                                - http
                              - enum:
                                - current
                                - old
                                - k8s
                                - owner
                                - http
                              description: From represents where this referenced object
                                are.
                              type: string
                            http:
                              description: Http means refer data from remote api.
                              properties:
                                method:
                                  description: Method as basic http method(e.g. GET
                                    or POST)
                                  type: string
                                params:
                                  additionalProperties:
                                    type: string
                                  description: Params represents the query value when
                                    it's get request and json body when it's a post
                                    request.
                                  type: object
                                url:
                                  description: URL as whole http url
                                  type: string
                              type: object
                            k8s:
                              description: K8s means refer another object from current
                                cluster.
                              properties:
                                apiVersion:
                                  description: APIVersion represents the API version
                                    of the target resources.
                                  type: string
                                kind:
                                  description: Kind represents the Kind of the target
                                    resources.
                                  type: string
                                labelSelector:
                                  description: A label query over a set of resources.
                                    If name is not empty, labelSelector will be ignored.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: A label selector requirement
                                          is a selector that contains values, a key,
                                          and an operator that relates the key and
                                          values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: operator represents a key's
                                              relationship to a set of values. Valid
                                              operators are In, NotIn, Exists and
                                              DoesNotExist.
                                            type: string
                                          values:
                                            description: values is an array of string
                                              values. If the operator is In or NotIn,
                                              the values array must be non-empty.
                                              If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This
                                              array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: matchLabels is a map of {key,value}
                                        pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions,
                                        whose key field is "key", the operator is
                                        "In", and the values array contains only "value".
                                        The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                name:
                                  description: Name of the target resource. Default
                                    is empty, which means selecting all resources.
                                  type: string
                                namespace:
                                  description: Namespace of the target resource. Default
                                    is empty, which means inherit from the parent
                                    object scope.
                                  type: string
                              required:
                              - apiVersion
                              - kind
                              type: object
                            path:
                              description: Path has different meaning, it represents
                                current object field path like "/spec/replica" when
                                From equals "current" and it also can be format like
                                "data.result.x.y" when From equals "http", it represents
                                the path in http response Only when From is owner(means
                                refer current object owner), the path can be empty.
                              type: string
                          type: object
                        message:
                          description: Message specify reject message when policy
                            hit.
                          type: string
                        value:
                          description: Value sets exact value for rule, like enum
                            or numbers
                          properties:
                            boolean:
                              description: Boolean only true or false can be recognized.
                              type: boolean
                            float:
                              description: 'Float as float but use string to store,
                                so please provide in comma (e.g. float: "1.2")'
                              type: string
                            floatSlice:
                              description: FloatSlice as a slice of float but using
                                string (e.g. ["1.2", "2.3"])
                              items:
                                description: Float64 is alias for float64 as string
                                type: string
                              type: array
                            integer:
                              description: Integer as an integer(int64)
                              format: int64
                              type: integer
                            integerSlice:
                              description: IntegerSlice as a slice of integer(int64)
                                (e.g. [1,2,3])
                              items:
                                format: int64
                                type: integer
                              type: array
                            string:
                              description: String as a string
                              type: string
                            stringMap:
                              additionalProperties:
                                type: string
                              description: StringMap as key-value set and both are
                                string.
                              type: object
                            stringSlice:
                              description: StringSlice as a slice of string(e.g. ["a","b"])
                              items:
                                type: string
                              type: array
                          type: object
                        valueRef:
                          description: ValueRef represents for value reference from
                            current or remote object. Need specify the type of object
                            and how to get it.
                          properties:
                            from:
                              allOf:
                              - enum:
                                - current
                                - old
                                - k8s
                                - owner
                                - http
                              - enum:
                                - current
                                - old
                                - k8s
                                - owner
                                - http
                              description: From represents where this referenced object
                                are.
                              type: string
                            http:
                              description: Http means refer data from remote api.
                              properties:
                                method:
                                  description: Method as basic http method(e.g. GET
                                    or POST)
                                  type: string
                                params:
                                  additionalProperties:
                                    type: string
                                  description: Params represents the query value when
                                    it's get request and json body when it's a post
                                    request.
                                  type: object
                                url:
                                  description: URL as whole http url
                                  type: string
                              type: object
                            k8s:
                              description: K8s means refer another object from current
                                cluster.
                              properties:
                                apiVersion:
                                  description: APIVersion represents the API version
                                    of the target resources.
                                  type: string
                                kind:
                                  description: Kind represents the Kind of the target
                                    resources.
                                  type: string
                                labelSelector:
                                  description: A label query over a set of resources.
                                    If name is not empty, labelSelector will be ignored.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: A label selector requirement
                                          is a selector that contains values, a key,
                                          and an operator that relates the key and
                                          values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: operator represents a key's
                                              relationship to a set of values. Valid
                                              operators are In, NotIn, Exists and
                                              DoesNotExist.
                                            type: string
                                          values:
                                            description: values is an array of string
                                              values. If the operator is In or NotIn,
                                              the values array must be non-empty.
                                              If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This
                                              array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: matchLabels is a map of {key,value}
                                        pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions,
                                        whose key field is "key", the operator is
                                        "In", and the values array contains only "value".
                                        The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                name:
                                  description: Name of the target resource. Default
                                    is empty, which means selecting all resources.
                                  type: string
                                namespace:
                                  description: Namespace of the target resource. Default
                                    is empty, which means inherit from the parent
                                    object scope.
                                  type: string
                              required:
                              - apiVersion
                              - kind
                              type: object
                            path:
                              description: Path has different meaning, it represents
                                current object field path like "/spec/replica" when
                                From equals "current" and it also can be format like
                                "data.result.x.y" when From equals "http", it represents
                                the path in http response Only when From is owner(means
                                refer current object owner), the path can be empty.
                              type: string
                          type: object
                      type: object
                  type: object
                type: array
            required:
            - validateRules
            type: object
        type: object
    served: true
    storage: true
